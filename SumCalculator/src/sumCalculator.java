public class sumCalculator {
    public static void main(String[] args) {
        int x = 10;
        int y = 3;
        int sum = getSum (x, y);
        System.out.println("Summ x + y = " +sum);
    }
    public static int getSum (int x, int y) {
        return x + y;
    }
}
